{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": [
    "pnpm.lock",
    "tsconfig.json",
    ".swcrc"
  ],
  "pipeline": {
    "build": {
      "outputs": [
        "dist/**"
      ],
      "dependsOn": [
        "^build"
      ]
    }
  }
}